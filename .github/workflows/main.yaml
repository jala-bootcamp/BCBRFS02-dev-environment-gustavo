name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v1
        with:
          node-version: 20.x
          cache: 'pnpm'
      - run: cd react-project
      - run: pnpm install --frozen-lockfile
      - run: pnpm run build
                  